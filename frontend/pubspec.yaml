name: supabase_vault
description: Supabase client vault and keep-alive manager

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  intl: ^0.19.0
  google_fonts: ^6.2.1

flutter:
  uses-material-design: true
